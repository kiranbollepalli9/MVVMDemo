<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
    <item name="android:textColor">@color/color_button_text</item>
    <item name="android:textColorHint">@color/color_button_text</item>
    <item name="android:backgroundTint">@color/color_button_text</item>
    <item name="android:layout_marginBottom">@dimen/base_margin</item>
  </style>
</resources>
